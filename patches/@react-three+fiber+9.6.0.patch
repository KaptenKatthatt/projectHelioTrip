diff --git a/node_modules/@react-three/fiber/dist/events-11ca7a92.cjs.prod.js b/node_modules/@react-three/fiber/dist/events-11ca7a92.cjs.prod.js
index 7ee6974..55d8f44 100644
--- a/node_modules/@react-three/fiber/dist/events-11ca7a92.cjs.prod.js
+++ b/node_modules/@react-three/fiber/dist/events-11ca7a92.cjs.prod.js
@@ -987,6 +987,44 @@ const createStore = (invalidate, advance) => {
       }
     }));
     const pointer = new THREE__namespace.Vector2();
+    function createFiberClock(autoStart = true) {
+      return {
+        autoStart,
+        startTime: 0,
+        oldTime: 0,
+        elapsedTime: 0,
+        running: false,
+        start() {
+          this.startTime = performance.now();
+          this.oldTime = this.startTime;
+          this.elapsedTime = 0;
+          this.running = true;
+        },
+        stop() {
+          this.getDelta();
+          this.running = false;
+          this.autoStart = false;
+        },
+        getElapsedTime() {
+          this.getDelta();
+          return this.elapsedTime;
+        },
+        getDelta() {
+          let diff = 0;
+          if (this.autoStart && !this.running) {
+            this.start();
+            return 0;
+          }
+          if (this.running) {
+            const newTime = performance.now();
+            diff = (newTime - this.oldTime) / 1000;
+            this.oldTime = newTime;
+            this.elapsedTime += diff;
+          }
+          return diff;
+        }
+      };
+    }
     const rootState = {
       set,
       get,
@@ -1007,7 +1045,7 @@ const createStore = (invalidate, advance) => {
       linear: false,
       flat: false,
       controls: null,
-      clock: new THREE__namespace.Clock(),
+      clock: createFiberClock(),
       pointer,
       mouse: pointer,
       frameloop: 'always',
diff --git a/node_modules/@react-three/fiber/dist/events-508aad4b.cjs.dev.js b/node_modules/@react-three/fiber/dist/events-508aad4b.cjs.dev.js
index c46d5e6..f8e61f4 100644
--- a/node_modules/@react-three/fiber/dist/events-508aad4b.cjs.dev.js
+++ b/node_modules/@react-three/fiber/dist/events-508aad4b.cjs.dev.js
@@ -987,6 +987,44 @@ const createStore = (invalidate, advance) => {
       }
     }));
     const pointer = new THREE__namespace.Vector2();
+    function createFiberClock(autoStart = true) {
+      return {
+        autoStart,
+        startTime: 0,
+        oldTime: 0,
+        elapsedTime: 0,
+        running: false,
+        start() {
+          this.startTime = performance.now();
+          this.oldTime = this.startTime;
+          this.elapsedTime = 0;
+          this.running = true;
+        },
+        stop() {
+          this.getDelta();
+          this.running = false;
+          this.autoStart = false;
+        },
+        getElapsedTime() {
+          this.getDelta();
+          return this.elapsedTime;
+        },
+        getDelta() {
+          let diff = 0;
+          if (this.autoStart && !this.running) {
+            this.start();
+            return 0;
+          }
+          if (this.running) {
+            const newTime = performance.now();
+            diff = (newTime - this.oldTime) / 1000;
+            this.oldTime = newTime;
+            this.elapsedTime += diff;
+          }
+          return diff;
+        }
+      };
+    }
     const rootState = {
       set,
       get,
@@ -1007,7 +1045,7 @@ const createStore = (invalidate, advance) => {
       linear: false,
       flat: false,
       controls: null,
-      clock: new THREE__namespace.Clock(),
+      clock: createFiberClock(),
       pointer,
       mouse: pointer,
       frameloop: 'always',
diff --git a/node_modules/@react-three/fiber/dist/events-760a1017.esm.js b/node_modules/@react-three/fiber/dist/events-760a1017.esm.js
index f4e89a7..de9d2e7 100644
--- a/node_modules/@react-three/fiber/dist/events-760a1017.esm.js
+++ b/node_modules/@react-three/fiber/dist/events-760a1017.esm.js
@@ -962,6 +962,44 @@ const createStore = (invalidate, advance) => {
       }
     }));
     const pointer = new THREE.Vector2();
+    function createFiberClock(autoStart = true) {
+      return {
+        autoStart,
+        startTime: 0,
+        oldTime: 0,
+        elapsedTime: 0,
+        running: false,
+        start() {
+          this.startTime = performance.now();
+          this.oldTime = this.startTime;
+          this.elapsedTime = 0;
+          this.running = true;
+        },
+        stop() {
+          this.getDelta();
+          this.running = false;
+          this.autoStart = false;
+        },
+        getElapsedTime() {
+          this.getDelta();
+          return this.elapsedTime;
+        },
+        getDelta() {
+          let diff = 0;
+          if (this.autoStart && !this.running) {
+            this.start();
+            return 0;
+          }
+          if (this.running) {
+            const newTime = performance.now();
+            diff = (newTime - this.oldTime) / 1000;
+            this.oldTime = newTime;
+            this.elapsedTime += diff;
+          }
+          return diff;
+        }
+      };
+    }
     const rootState = {
       set,
       get,
@@ -982,7 +1020,7 @@ const createStore = (invalidate, advance) => {
       linear: false,
       flat: false,
       controls: null,
-      clock: new THREE.Clock(),
+      clock: createFiberClock(),
       pointer,
       mouse: pointer,
       frameloop: 'always',
