# Required for production builds unless your host sets CF_PAGES_URL, VERCEL_URL,
# or Netlify deploy URL env vars. Origin without a trailing slash — used so
# og:image and twitter:image are absolute URLs (Facebook / X reject relative paths).
VITE_PUBLIC_SITE_URL=
