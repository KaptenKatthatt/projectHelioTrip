# Optional: site origin without trailing slash, e.g. https://your-domain.com
# Production builds replace __PUBLIC_SITE_URL__ in index.html so og:image and
# twitter:image use absolute URLs (recommended for Facebook / X link previews).
VITE_PUBLIC_SITE_URL=
